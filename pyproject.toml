[project]
name = "project-chimera"
version = "0.1.0"
description = "Agentic Infrastructure for Autonomous Influencer Factory"
requires-python = ">=3.10"

dependencies = [
  "sqlalchemy>=2.0",
  "psycopg2-binary",
  "pgvector",
  "pytest"
]
